# 🛡️ ShadeWall IPS 

### Система активной сетевой обороны и визуализации угроз в реальном времени

ShadeWall — это комплексное защитное решение, объединяющее функции системы предотвращения вторжений (IPS) и сети ловушек (Honeypot). Проект разработан для автоматического обнаружения сетевых атак, мгновенной блокировки злоумышленников и наглядной визуализации инцидентов.

---

## 🚀 Основные возможности

1.  Anti-DoS: Защита от флуда. Если количество пакетов от одного IP (TCP, UDP или ICMP) превышает лимит, источник автоматически блокируется.
2.  Honeypot (Порты-ловушки): При попытке обращения к скрытым портам (например, 445, 3389) атакующий мгновенно попадает в бан.
3.  GeoIP Visualization: Карта мира в реальном времени показывает, откуда совершаются атаки.
4.  Dynamic Firewall: Автоматическое управление правилами iptables на уровне ядра Linux.
5.  Smart Whitelist: Интерактивное управление доверенными адресами через веб-панель.
6.  Telegram notifications: Автоматическая отправка уведомлений об атаке.

---

## 📂 Структура проекта

* main.py — Движок системы (анализ трафика и блокировка).
* dashboard.py — Веб-интерфейс на Streamlit.
* config.yaml — Настройки (интерфейсы, лимиты, порты-ловушки).
* whitelist.txt — Список разрешенных IP.
* requirements.txt — Список зависимостей проекта.
* shadewall_log.json — Логи атак в формате JSON.

---

## ⚙️ Установка и запуск (с нуля)

Система требует Python 3.10+ и установленный iptables.

### 1. Подготовка папки и окружения
Откройте терминал в папке проекта:

```bash
# Для Ubuntu/Debian
sudo apt update && sudo apt install python3 python3-venv git iptables
# Создаем виртуальное окружение
python -m venv venv

# Активируем его
source venv/bin/activate

#Скачиваем зависимости
pip install --upgrade pip
pip install -r requirements.txt

#Перед запуском настройте config.yaml! Обязательно уберите порт SSH из списка honey_ports или добавьте IP своего провайдера в whitelist
#Запуск проекта
#В одном терминале - запуск скрипта защиты
sudo ./venv/bin/python main.py
#В другом терминале - запуск дашборда
sudo ./venv/bin/python -m streamlit run dashboard.py
```

---
## ⚖️ Лицензия (License)

Этот проект распространяется под лицензией MIT. 
Это значит, что вы можете свободно:
* Использовать код в личных и коммерческих целях.
* Модифицировать и изменять логику системы.
* Распространять копии программного обеспечения.

*Единственное условие — сохранение авторства оригинального разработчика.*

---

Автор проекта: steal_pork\n
Разработано для регионального этапа ВСОШ по Информационной безопаности

## 🛡️ Безопасность и Ответственность

ВАЖНО: Данное ПО является инструментом сетевого администрирования. 
* Автор не несет ответственности за любой ущерб, вызванный неправильной настройкой правил iptables.
* Запрещено использование системы для несанкционированного блокирования доступа к сторонним ресурсам.
* Помните, что запуск main.py без корректно настроенного whitelist.txt может привести к потере доступа к вашему собственному серверу по SSH (порт 22).
